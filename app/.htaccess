RewriteEngine On

# Si le fichier ou le dossier demandé n'existe pas physiquement...
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# ... alors on redirige toute la requête vers index.php
RewriteRule ^(.*)$ index.php [QSA,L]